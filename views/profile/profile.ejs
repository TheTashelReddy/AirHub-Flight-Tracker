<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
    .profile-card {
      max-width: 300px;
      margin: 0 auto;
      padding: 20px;
      border: 1px solid #4cacff;
      border-radius: 8px;
      background-color: #f9f9f9;
    }
    .profile-img {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      margin: 0 auto;
      overflow: hidden;
    }
    .profile-img img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    </style>
</head>
<body>
  <h1 class="mb-5 text-center">Welcome <%= user.name %>!</h1>
  <div class="text-center">
    <form action="/logout" method="POST" class="d-inline">
      <button type="submit" class="btn btn-warning mt-3 mb-3">Logout</button>
    </form>
    <a href="/gallery"><button class="btn btn-primary">Share Trips</button></a>
  </div>
<div class="container mt-5 mb-5">
  <div class="profile-card">
      <ul class="list-group list-group-flush">
          <li class="list-group-item">Name: <span id="displayName"><%= user.name %></span></li>
          <li class="list-group-item">Email: <span id="displayEmail"><%= user.email %></span></li>
          <li class="list-group-item">Status: <span id="displayStatus"><%= user.status %></span></li>
          <li class="list-group-item">Plan: <span id="displayPlan"><%= user.plan %></span></li>
          <li class="list-group-item">Preferences: <span id="displayPreferences"><%= user.preferences %></span></li>
          <li class="list-group-item">Notification Preferences: <span id="displayNotificationPreferences"><%= user.notificationPreferences %></span></li>
          <li class="list-group-item">Language: <span id="displayLanguageSettings"><%= user.languageSettings %></span></li>
      </ul> 
      <div class="text-center">
          <a href="/settings"><button type="button" class="btn btn-success mr-3">Update Profile</button></a>
          <a href="/delete"><button type="button" class="btn btn-danger">Delete Profile</button></a>
      </div>
  </div>
</div>


</body>
</html>


